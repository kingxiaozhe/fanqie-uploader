OpenAI Codex v0.144.5
--------
workdir: /Users/zero/MyCode/fanqie-uploader
model: gpt-5.6-sol
provider: openai
approval: never
sandbox: read-only
reasoning effort: high
reasoning summaries: none
session id: 019f7ad8-373b-7971-b1bf-03585608d998
--------
user
只审查下面这段 diff（一个零依赖 Chrome MV3 扩展新增的章节文件编码自适应函数 decodeChapterBytes 及其接入与测试）。不要读其它文件、不要探索仓库。硬约束：零第三方依赖、纯逻辑函数须顶层具名 function、报错文案不含裸花括号。只报有具体失败场景的问题（什么字节输入→什么错误结果），按严重度排序；没有真实问题就只回复『零发现』。特别看：编码误判风险、BOM/空输入边界、测试是否安慰剂。

DIFF:
diff --git a/popup/popup.js b/popup/popup.js
index e731a8d..9be629c 100644
--- a/popup/popup.js
+++ b/popup/popup.js
@@ -269,7 +269,7 @@ async function onFolderPicked(e) {
 
   tasks = [];
   for (const f of files) {
-    const raw = (await f.text()).replace(/
/g, "
").trim();
+    const raw = decodeChapterBytes(await f.arrayBuffer()).replace(/
/g, "
").trim();
     const { title, content } = splitTitleBody(raw, f.name);
     const chapterNumber = numFromName(f.name) || numFromText(raw);
     tasks.push({
@@ -505,6 +505,27 @@ function cnToInt(s) {
   }
   return total + cur;
 }
+
+// 把文件字节按编码解出文本：BOM 优先，无 BOM 用 UTF-8 严格解码试探，失败回退 GBK。
+// 老作者 / 记事本导出的 txt 常是 GBK，直接 f.text() 只按 UTF-8 解码会整章乱码。
+// 只用浏览器原生 TextDecoder（含 gbk 标签，映射 GB18030 解码器），零依赖。
+function decodeChapterBytes(buf) {
+  const bytes = new Uint8Array(buf);
+  // BOM 判定：EF BB BF=UTF-8；FF FE=UTF-16LE；FE FF=UTF-16BE
+  if (bytes.length >= 3 && bytes[0] === 0xef && bytes[1] === 0xbb && bytes[2] === 0xbf)
+    return new TextDecoder("utf-8").decode(bytes.subarray(3));
+  if (bytes.length >= 2 && bytes[0] === 0xff && bytes[1] === 0xfe)
+    return new TextDecoder("utf-16le").decode(bytes.subarray(2));
+  if (bytes.length >= 2 && bytes[0] === 0xfe && bytes[1] === 0xff)
+    return new TextDecoder("utf-16be").decode(bytes.subarray(2));
+  // 无 BOM：先 UTF-8 严格解码，遇非法字节序列抛错 → 判为非 UTF-8，回退 GBK
+  try {
+    return new TextDecoder("utf-8", { fatal: true }).decode(bytes);
+  } catch (_) {
+    return new TextDecoder("gbk").decode(bytes);
+  }
+}
+
 // 把一篇文档拆成「标题 + 正文」，并清理 Markdown 标记
 // 规则：首个非空行当标题（清掉 # / ** 等）；其余为正文。
 //      若首行太长(>40字)更像正文，则用文件名当标题、整篇当正文。
diff --git a/tests/logic.test.cjs b/tests/logic.test.cjs
index fba12e0..5402112 100644
--- a/tests/logic.test.cjs
+++ b/tests/logic.test.cjs
@@ -48,6 +48,7 @@ eval(extract(up, "sameTitle"));
 eval(extract(popup, "cnToInt"));
 eval(extract(popup, "numFromName"));
 eval(extract(popup, "numFromText"));
+eval(extract(popup, "decodeChapterBytes"));
 
 let P = 0, F = 0;
 const ok = (n, c, g) => { if (c) { P++; console.log("✅ " + n); } else { F++; console.log("❌ " + n + "  → " + JSON.stringify(g)); } };
@@ -209,5 +210,20 @@ ok("标题剥前缀: 剥空回退原标题", pureTitle("第二章") === "第二
 ok("章节号兜底: extractNumber 第二十章 → 20", extractNumber("第二十章 你好") === 20);
 ok("去重归一: 中文/阿拉伯数字前缀视为同章", sameTitle("第2章 重算这笔账", "第二章 重算这笔账"));
 
+// ---- 章节文件编码自适应：UTF-8 优先，GBK/UTF-16 兜底（修复 GBK 稿导入整章乱码）----
+const abuf = (arr) => Uint8Array.from(arr).buffer;
+const U8 = [0xe4, 0xbd, 0xa0, 0xe5, 0xa5, 0xbd, 0xe4, 0xb8, 0x96, 0xe7, 0x95, 0x8c]; // 你好世界 UTF-8
+const GBK = [0xc4, 0xe3, 0xba, 0xc3, 0xca, 0xc0, 0xbd, 0xe7];                          // 你好世界 GBK
+const U16LE = [0x60, 0x4f, 0x7d, 0x59, 0x16, 0x4e, 0x4c, 0x75];                        // 你好世界 UTF-16LE
+const U16BE = [0x4f, 0x60, 0x59, 0x7d, 0x4e, 0x16, 0x75, 0x4c];                        // 你好世界 UTF-16BE
+ok("编码: UTF-8 中文", decodeChapterBytes(abuf(U8)) === "你好世界");
+ok("编码: GBK 兜底(非法UTF-8→GBK)", decodeChapterBytes(abuf(GBK)) === "你好世界", decodeChapterBytes(abuf(GBK)));
+ok("编码: UTF-8+BOM 去BOM", decodeChapterBytes(abuf([0xef, 0xbb, 0xbf, ...U8])) === "你好世界");
+ok("编码: UTF-16LE(FF FE)", decodeChapterBytes(abuf([0xff, 0xfe, ...U16LE])) === "你好世界");
+ok("编码: UTF-16BE(FE FF)", decodeChapterBytes(abuf([0xfe, 0xff, ...U16BE])) === "你好世界");
+ok("编码: 纯ASCII不误判", decodeChapterBytes(abuf([...Buffer.from("Hello, world.")])) === "Hello, world.");
+ok("编码: 中英混排UTF-8不误判为GBK", decodeChapterBytes(abuf([...Buffer.from("第1章 Hi 世界", "utf8")])) === "第1章 Hi 世界");
+ok("编码: 空内容不崩", decodeChapterBytes(abuf([])) === "");
+
 console.log(`
内容脚本逻辑：${P}/${P + F} 通过`);
 process.exit(F ? 1 : 0);

codex
- 中：无 BOM 的 GBK 可被误判为合法 UTF-8。字节 `C2 A1` 在 GBK 中是“隆”，但也是合法 UTF-8，会被解成“¡”，不会触发 GBK 回退。现有 GBK 测试仅覆盖“恰好包含非法 UTF-8”的样本，未覆盖这种歧义输入，因此无法验证“GBK 自适应”的关键误判风险。
tokens used
8,526
- 中：无 BOM 的 GBK 可被误判为合法 UTF-8。字节 `C2 A1` 在 GBK 中是“隆”，但也是合法 UTF-8，会被解成“¡”，不会触发 GBK 回退。现有 GBK 测试仅覆盖“恰好包含非法 UTF-8”的样本，未覆盖这种歧义输入，因此无法验证“GBK 自适应”的关键误判风险。

---
## 处置（作者回应，r1 收口）
- Codex 1 条「中」：无 BOM GBK 恰为合法 UTF-8 会误判为 UTF-8，测试未覆盖歧义。
- 判定：探测算法不改——这是 design/requirements 已记录的已知边界（UTF-8 fatal 试探是标准做法，真实整章 GBK 稿几乎不可能全程合法 UTF-8）。采纳其对测试的批评：新增用例锁定该已知行为（C2 A1 → ¡），防安慰剂。
- 无逻辑改动，未触发 r2。
